const user = {
    name: 'SAcin',
    email: 'sachin@gmail.com',
    isActive: true,
}

const createUser = ({ name: string, isPaid: boolean }) => {}

const user2 = { name: 'sachin', isPaid: false, email: 's@gmai.com' }
createUser(user2)

const createCourse = (): { name: string; price: number } => {
    return { name: 'schin', price: 29 }
}

export {}
